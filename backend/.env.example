# Backend env
GOOGLE_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-flash

SUPABASE_URL=https://mfhushphigaxqrlknxvr.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_VRZFCPCgxyQAYLkYPt0MEQ_RN54bQY5
SUPABASE_JWT_SECRET=your_jwt_secret

# Community uploads
# If SUPABASE_URL + SUPABASE_SERVICE_KEY are set, uploads go to
# Supabase Storage. Otherwise they fall back to local disk at
# backend/data/pdfs/community/ (lost on Render redeploys, fine for demos).

CHROMA_PERSIST_DIR=./chroma_db
DATA_DIR=./data/pdfs

# Optional: change to True to use deterministic offline mode (no Gemini calls)
OFFLINE_MODE=False
